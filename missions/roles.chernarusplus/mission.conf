# The "real" roles mission runs on stable: 1.30 Experimental has zombie hit and door
# interaction bugs (see roles-exp for the 1.30 variant with motorbikes).
GAME=stable

# VehicleShootingAnywhere is our own, unsigned mod, so the server must not insist on signatures.
VERIFY_SIGNATURES=0
