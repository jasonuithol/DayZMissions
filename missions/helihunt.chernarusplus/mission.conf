# DayZ Expansion isn't available for 1.30 Experimental yet, so this one runs on stable.
GAME=stable

# HeliHuntCompat is our own, unsigned mod, so the server must not insist on signatures.
VERIFY_SIGNATURES=0
